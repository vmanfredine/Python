"""
# and (E): Retorna True se ambas as condições forem verdadeiras.
    idade >= 18 and tem_carteira == True.

# or (OU): Retorna True se pelo menos uma das condições for verdadeira.
    dia == 'Sábado' or dia == 'Domingo'.

#not (NÃO): Inverte o valor booleano de uma expressão (de True para False, e vice-versa).
    not (idade < 18) (equivalente a idade >= 18). 
"""